/**
 * Tuning Worker — Relayhouse Phase 0 (no-op stub)
 *
 * Phase 5+: Weekly tuning-runner (BullMQ). Builds tuning agenda
 * from confidence < 0.7 conversations, schedules call, generates
 * accuracy reports, handles KB diff sweeper.
 */
console.log('[tuning] worker running (no-op stub)');
