/**
 * KB Ingester Worker — Relayhouse Phase 0 (no-op stub)
 *
 * Phase 1+: Accepts { kind, value } → fetch source → chunk →
 * embed via text-embedding-3-small → store in kb_chunks.
 * Idempotent on (kbId, hash).
 */
console.log('[kb-ingester] worker running (no-op stub)');
